# ── secrets (NEVER commit) ─────────────────────────────
.env
calendar-url.txt

# ── personal config (copy config.example.json → config.json) ──
config.json

# ── your calendar data & runtime state ─────────────────
cache/
events.json
state.json
runtime/
logs/
stopped
ONBOARDING.md

# ── IDE ────────────────────────────────────────────────
.idea/

# ── build artifacts / environment ──────────────────────
venv/
bin/
__pycache__/
*.pyc
*.mp3
.DS_Store
